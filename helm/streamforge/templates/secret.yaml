{{- range $name, $svc := .Values.services }}
{{- if $svc.secrets }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}-secret
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $name }}
type: Opaque
stringData:
  {{- range $key, $val := $svc.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}
{{- end }}

